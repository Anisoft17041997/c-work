#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int isPal(long N);

    return 0;
}

int isPal(long N)
{
    char *CH;
    int i = 0;
    int j = strlen(CH);
    ltoa(N,CH,10);

    while(*(CH+i) = )
    return 0;
}
